인스턴스 변수 - 필드
MarketGoods 클래스는 아래와 같은 인스턴스 변수들을 포함한다
1. string name(상품의 이름)
2. int retalPrice(상품의 출시 가격)
3. int discountRate(상품의 할인율)
모두 private로 설정!

생성자
두개의 생성값을 가지고 있다.
1. 세 개의 파라미터를 받는 생성자
2. name, retalPrice 등 2개의 파라미터를 받는 생성자

getter / setter 메소드
1. name, retalPrice 는 getter 메소드만 만든다. 값을 지정해주는 것은 생성자에서 완료한다.
2. discountRate 바뀔 수 있으므로 getter와 setter 모두 만들어준다.

getDiscountprice() 메소드
할인값을 리턴하는 메소드이다.
예를 들어 retalPrice 10000이고, discountRate가 20이면, 8000을 리턴한다.
